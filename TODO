* qemu image and boot. no boot loader, EFI only still.
* kernel with embedded cmdline flags and ideally the intel microcode
* replace busybox with sbase or rust core utils plus a minimal shell and an init
